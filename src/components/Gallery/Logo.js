import React from 'react';
import Star from '../../images/logo.svg';

const Logo = () => (
  <div className="brand">
    <div className="logo">
      <Star />
    </div>
  </div>
);

export default Logo;
