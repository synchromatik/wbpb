import React from 'react';
import ReactDOM from 'react-dom';
import './styles/style.scss';

const template = React.createElement('p', {}, 'React');

ReactDOM.render(template, document.getElementById('root'));
